upstream web {
  ip_hash;
  server web:8000;
}

server {

    location /static/ {
        autoindex on;
        alias /static/;
    }

    location / {
        proxy_pass http://ng.theo.computer/;
    }
    listen 80;
    server_name ng.theo.computer;
}
