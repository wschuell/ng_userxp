{% load static %}
{% load i18n %}


    $('#home').tooltip({
      title : current_lang.btn_home,
      placement : "right auto",
      trigger: "hover focus",
    });

    $('#groop_3').tooltip({
      title : current_lang.groop_3,
      placement : "right auto",
      trigger: "hover focus",
    });

    $('#groop_5').tooltip({
      title : current_lang.groop_5,
      placement : "right auto",
      trigger: "hover focus",
    });
